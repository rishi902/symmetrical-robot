# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
pyyaml>=6.0

# Machine learning
scikit-learn>=1.3
xgboost>=2.0
lightgbm>=4.0

# Tuning
optuna>=3.5
optuna-integration[xgboost]>=3.5   # XGBoostPruningCallback moved out of the main optuna package

# Explainability
shap>=0.44

# Graphs
networkx>=3.0

# Plotting
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18

# Dashboard
streamlit>=1.30

# Data download
kaggle>=1.6

# Notebooks
jupyter>=1.0
ipykernel>=6.0

# Testing
pytest>=7.4

# ---------------------------------------------------------------
# GPU extras (Colab only, do NOT install on a laptop without CUDA).
# Install these by following the official RAPIDS instructions for the
# CUDA version Colab is running, rather than pinning versions here:
#   cudf, cuml, cugraph or nx-cugraph
# XGBoost above already supports GPU via device="cuda".
# ---------------------------------------------------------------
